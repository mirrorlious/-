# 任务：段落菜单显示“已缓存”状态

- 时间：2026-07-27
- 执行者：ChatGPT
- 状态：部分完成
- 项目：杨的阅读器
- 仓库：`mirrorlious/-`
- 工作分支：`feat/reader-annotations-v2`
- GitHub 上传：是，用户明确要求修改段落菜单缓存提示

## 1. 用户原始要求

段落工具菜单中，已经生成并缓存在当前页面状态中的段落翻译、长难句拆解等结果，应在菜单右侧显示小字“已缓存”，而不是留空或只显示勾号。

## 2. 任务目标

- 已存在段落翻译时显示“已缓存”。
- 已存在长难句拆解时显示“已缓存”。
- 已存在段落练习时显示“已缓存”。
- 已存在外教领读音频时显示“已缓存”。
- 加载状态、短段落提示和未生成状态保持原行为。
- 点击缓存项继续复用现有结果，不触发重复模型请求。

## 3. 明确不做

- 不修改缓存存储结构。
- 不修改模型调用、API、OCR、PDF或批量解析。
- 不改变段落菜单布局和功能顺序。
- 不修改选中文字交互。
- 不合并到 `main`。

## 4. 审计结果

- 段落菜单已经通过 `finalTranslationToShow`、`analysisData`、`quizData` 和 `audioUrl` 判断是否有现成结果。
- 当前右侧状态主要依赖 `showTranslation/showAnalysis/showQuiz`，折叠结果后会留空；展开时只显示勾号，不能明确告诉用户结果会从缓存读取。
- 各处理函数已经在数据存在时直接复用结果，因此本轮只需要调整状态文案。

## 5. 实施计划

1. 将翻译、长难句、练习和外教领读的已有结果状态统一改为小字“已缓存”。
2. 保留“处理中 / 解构中 / 生成中 / 请求中 / 本段较短 / 内容不足 / 调用模型”等状态。
3. 执行静态标记、`git diff --check` 和 Babel JSX 解析。
4. 删除一次性脚本和工作流。

## 6. 验收标准

- 已生成过翻译的段落，菜单右侧显示“已缓存”。
- 已生成过长难句拆解的段落，菜单右侧显示“已缓存”。
- 已生成过练习或外教音频时同样显示“已缓存”。
- 折叠结果后“已缓存”仍然可见。
- 再次点击不会重复调用模型。
- Babel JSX 解析通过。

## 7. 实际修改

- 段落翻译存在 `finalTranslationToShow` 时，菜单右侧显示小字“已缓存”。
- 长难句结果存在 `analysisData` 时，菜单右侧显示小字“已缓存”。
- 段落练习存在 `quizData` 时，菜单右侧显示小字“已缓存”。
- 外教领读存在 `audioUrl` 时，菜单右侧显示小字“已缓存”。
- 缓存提示不再依赖结果是否当前展开，因此折叠后仍可识别。
- 各处理函数原有缓存复用逻辑保持不变。

## 8. 测试

- 四类缓存状态源码替换：通过。
- 现有结果复用分支保留检查：通过。
- `git diff --check`：由一次性工作流执行。
- Babel JSX 解析：由一次性工作流执行。
- 浏览器视觉验收：等待用户本地复测。

## 9. 回滚方式

回退本次功能提交即可；不涉及 `main`。
