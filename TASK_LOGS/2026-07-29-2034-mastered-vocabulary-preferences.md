# 任务：已掌握词取消划线与 Markdown 携带

- 时间：2026-07-29 20:34
- 执行者：Codex
- 状态：已完成
- 本地路径：`D:\01_project\杨的阅读器`
- GitHub 上传：否

## 1. 用户原始要求

用户点击已划线单词后，可将过于简单的词设为不再划线；可以单独或全部恢复默认；不引入账户和自动跨设备同步，设置随现有 Markdown 备份手动迁移。用户已确认可以执行。

## 2. 任务目标

- 增加本地、全局生效的“已掌握词”列表。
- 词卡提供“不再划线”与即时撤销。
- 在现有个人词库界面管理和恢复已掌握词。
- 将设置作为可选字段写入现有 Markdown 数据块，并在导入时由用户确认是否合并。
- 保持旧 Markdown、现有词库、高亮范围和静态部署兼容。

## 3. 本次范围

- 当前可维护源码 `src/App.jsx` 中的词汇匹配、高亮、词卡、个人词库弹窗、本地状态和 Markdown 导入导出。
- 与该功能直接相关的自动测试和手工验收说明。

## 4. 明确不做

- 不增加账户、Firebase 或云端同步。
- 不改默认词库 JSON。
- 不处理短语隐藏。
- 不新增独立备份文件格式。
- 不介入当前工作区中正在进行的生产构建迁移及其他无关改动。
- 不上传 GitHub。

## 5. 审计结果

- 当前可维护源码已由另一项在途任务迁移到 `src/App.jsx`；根目录 `index.html` 为 Vite 入口。
- 词形匹配由 `getLemmaMatches` 完成，并返回稳定的 `word`（lemma），可直接作为已掌握词键。
- 小型偏好统一保存在 `yang-reader-state-v1` 的 `localStorage` 状态中。
- 文章 Markdown 使用 `yang-reader-data` Base64 JSON 数据块，当前 schema version 为 1；导入逻辑可兼容缺少新字段的旧文件。
- 当前工作区已有大量与生产构建迁移相关的未提交文件，本任务只对自己的源码、测试和日志做增量修改。

## 6. 涉及文件

- `src/App.jsx`
- 与本功能直接相关的测试文件（审计后确定）
- 本任务日志

## 7. 实施计划

1. 增加规范化、合并和本地持久化的已掌握词状态。
2. 在单词高亮渲染前按 lemma 排除已掌握词。
3. 在词卡增加“不再划线”操作和可撤销提示。
4. 在个人词库弹窗增加已掌握词搜索、逐个恢复和全部恢复。
5. Markdown 数据块升级为兼容性的 schema v2，可选携带个人词汇偏好；导入时一次确认后合并。
6. 增加针对核心纯逻辑和构建产物的验证，执行测试、构建和差异检查。

## 8. 实际修改

- `src/App.jsx`
  - 新增规范化、去重的全局已掌握词状态，保存到 `yang-reader-state-v1.vocabularyPreferences`。
  - 词汇匹配得到 lemma 后先检查已掌握词集合，命中时按普通文字渲染。
  - 单词词卡新增“这个词太简单，不再划线”，操作后立即刷新全文，并提供 6 秒撤销入口。
  - 个人词库弹窗新增已掌握词数量、搜索、逐词恢复和全部恢复默认。
  - 移除该弹窗中不符合项目事实的 Firebase 同步文案，明确数据保存在当前浏览器并可随 Markdown 携带。
  - Markdown schema version 从 1 升至 2；实际导出时注入可选 `portablePreferences.vocabulary` 快照，不修改文章 IndexedDB 记录。
  - 导入多份 Markdown 时汇总并去重偏好，只在存在本机新增词时询问一次是否合并；取消后仍正常恢复文章。
- `tests/unit/production-entry.test.js`
  - 增加本地偏好、可携带字段和高亮排除契约检查。
- `tests/e2e/reader-baseline.spec.js`
  - 增加点击 `absolute` 取消划线、本地持久化和撤销恢复的浏览器回归。

## 9. 数据与配置迁移

- 本地新增可选字段：`yang-reader-state-v1.vocabularyPreferences = { version: 1, ignoredLemmas: [] }`。
- Markdown 数据块 schema version 升至 2，并可选包含 `portablePreferences.vocabulary`。
- 旧本地状态缺少该字段时按空列表处理。
- 旧 Markdown 缺少该字段时继续按原流程导入，不触发偏好确认。
- 未修改默认词库 JSON、IndexedDB version 或 object store。

## 10. 测试

### 自动测试

- `npm run test:unit`：通过，1 个文件、4 项测试全部通过。
- `npm run build`：通过，Vite 45 个模块构建完成，静态资源脚本确认 3 份词库 JSON 已加入产物。
- `npm run test:e2e`：通过，4 项浏览器测试全部通过，其中包含已掌握词新增回归。
- `git diff --check`：通过；仅出现工作区既有文件的 CRLF 提示，无空白错误。

### 手工验收

- 浏览器自动化已实际点击示例文章中的 `absolute`，确认划线立即消失。
- 已检查 `localStorage` 中保存 `ignoredLemmas: ["absolute"]`。
- 已点击撤销，确认该词重新按默认规则划线。
- 个人词库管理界面、Markdown 导入确认文案仍建议由用户进行一次视觉验收。

## 11. 风险与已知问题

- 现有词形还原是启发式规则，本功能复用其结果，不额外扩大词形识别范围。
- 每篇导出的 Markdown 会携带导出时的全局偏好快照；导入时必须显式确认，避免静默改变本机设置。
- 工作区存在另一项在途构建迁移，修改时需防止覆盖并发变化。
- 导入旧备份并确认合并，可能重新加入用户后来恢复过的词；确认对话明确显示新增数量，用户可以取消或随后恢复。
- 本轮未增加针对下载文件与文件选择器的端到端测试，相关序列化路径由构建和源码契约检查覆盖。

## 12. 未完成项

- 用户侧视觉验收：个人词库弹窗在窄屏下的滚动、搜索和恢复操作。
- 用户侧手工验收：在另一浏览器中导入新 MD，并分别验证“合并”和“只恢复文章”。

## 13. 回滚方式

移除 `src/App.jsx` 中本任务新增的 mastered vocabulary 状态、词卡按钮、管理区域和 `portablePreferences` 导入导出逻辑，并删除两项对应测试增量；不要回退工作区中的生产构建迁移及其他任务改动。

## 14. 最终结论

本地实现、单元测试、生产构建和浏览器回归均已完成。没有账户、云端或自动同步；已掌握词仅保存在本地，并由用户选择是否随 Markdown 备份合并恢复。

GitHub：未上传，等待用户本地验收。
