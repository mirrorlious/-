# 任务：合并右侧结果面板与全文工具整理

- 时间：2026-07-27
- 执行者：ChatGPT
- 状态：开发中
- 项目：杨的阅读器
- 仓库：`mirrorlious/-`
- 本地路径：`D:\01_project\杨的阅读器`
- 工作分支：`feat/reader-annotations-v2`
- GitHub 上传：是，用户明确要求继续实施合并后的第三、第四阶段

## 1. 用户原始要求

第一阶段段落 `⋯` 菜单验收通过。继续实施原第三、第四阶段，但将两阶段合并：统一右侧学习结果面板，并把全文级操作集中到顶部“全文工具”菜单。

## 2. 本次范围

- 右侧标签统一为“全文结构 / 精读结果 / 学习笔记”。
- 段落翻译、长难句拆解、段落练习的结果均可进入右侧“精读结果”。
- 精读结果明确显示当前对象、操作类型和原文片段。
- 顶部增加“全文工具”菜单，集中现有全文翻译、全文逻辑、思维导图和学习笔记入口。
- 全文翻译、全文逻辑完成后进入右侧精读结果；思维导图进入全文结构。
- 点击相关操作时自动切换到分栏并打开对应右侧标签。
- 已存在的结果优先复用，不重复调用模型。

## 3. 明确不做

- 不实施选区工具条分级；该部分仍作为独立后续阶段。
- 不新增当前项目中不存在的全文朗读、全文出题或全文总结模型接口。
- 不修改 OCR、PDF、API 服务商、批注数据结构、阅读库和 Firebase。
- 不删除正文中的现有段落结果，右侧面板先作为统一查看与导航容器。
- 不合并到 `main`。
- 不读取或修改 Miki 工程及仓库内其他无关历史文件。

## 4. 当前审计

- 当前右侧已有 `outline / analysis / notes` 三个标签，但“精读结果”只展示长难句简表，无法展示段落翻译、练习或全文翻译。
- 当前段落长难句会主动打开右侧面板；段落翻译和练习仍只显示在正文中。
- 顶部仍单独放置“全文逻辑”按钮；全文翻译没有明确入口，思维导图入口位于文章底部。
- 已有 `fullTranslations`、`globalLogicData`、`fullMapData` 和段落结果状态，可在不新增模型接口的前提下完成整理。
- 一次性工作流已登记；已修正首轮静态标记误判，本提交触发重新实施。

## 5. 实施计划

1. 将右侧结果状态改为带 `kind / title / paragraphIndex / sourceText / data` 的统一对象。
2. 段落翻译、长难句和练习在已有结果或新结果生成后同步打开右侧结果。
3. 增加右侧结果渲染器，覆盖段落翻译、段落句法、段落练习、全文翻译和全文逻辑。
4. 增加顶部“全文工具”菜单及外部点击、Esc 关闭逻辑。
5. 将全文翻译、全文逻辑、思维导图和学习笔记接入统一菜单。
6. 完成静态标记、Babel JSX 和 `git diff --check` 验证后删除一次性脚本与工作流。

## 6. 验收标准

- 顶部不再单独突出“全文逻辑”，改为稳定的“全文工具”入口。
- 全文工具菜单点击后保持打开，点击空白或按 Esc 关闭。
- 段落翻译、长难句、练习均能在右侧精读结果显示。
- 右侧结果显示“第几段 / 全文”、操作类型和原文片段。
- 已生成结果再次打开时不重复调用模型。
- 全文翻译、全文逻辑、思维导图分别进入正确的右侧标签。
- 学习笔记入口直接打开右侧笔记标签。
- 原段落菜单、批注、PDF、结构树编辑保持可用。
- Babel JSX 解析通过。

## 7. 实际修改

开发中。

## 8. 测试

待执行。

## 9. 未完成项

开发中。

## 10. 回滚方式

回退本次功能提交即可；不涉及 `main`。
