# 任务：将段落悬停按钮改为右侧固定菜单入口

- 时间：2026-07-27
- 执行者：ChatGPT
- 状态：审计中
- 项目：杨的阅读器
- 仓库：`mirrorlious/-`
- 本地路径：`D:\01_project\杨的阅读器`
- 工作分支：`feat/reader-annotations-v2`
- GitHub 上传：是，用户明确要求开始实施第一阶段

## 1. 用户原始要求

第一阶段先处理段落交互：不再通过鼠标悬停在段落底部撑开多枚大按钮；每段右侧保留低存在感但可发现的 `⋯` 入口，点击后打开固定浮层菜单。悬停只增强入口可见度，不再作为唯一触发方式。第三、第四阶段后续合并处理，本次不实施。

## 2. 本次范围

- 移除段落底部悬停展开的横向大按钮组。
- 每段右侧加入稳定的 `⋯` 按钮。
- 点击后打开不占文档流、不改变段落高度的浮层菜单。
- 菜单继续调用现有段落翻译、长难句、本地朗读、外教领读、出题和复制逻辑。
- 点击空白、按 `Esc` 或再次点击入口关闭。
- 同一时刻只保留当前段落菜单。
- 短段落入口保持存在；不适合的长难句和出题功能置灰并解释，而不是凭空消失。
- 桌面端默认低透明度、段落悬停或键盘聚焦时变清晰；触屏端保持可见。

## 3. 明确不做

- 不实施选区工具条分级。
- 不重构右侧精读结果面板。
- 不整理全文工具菜单。
- 不修改 OCR、PDF、API、缓存、批注数据结构和结构树。
- 不增加复杂多段选择。
- 不合并到 `main`。
- 不读取或修改 Miki 工程；仓库中的无关历史文件全部忽略。

## 4. 审计结果

- 实际运行入口：根目录 `index.html`。
- `Paragraph` 组件当前以 `onMouseEnter/onMouseLeave` 修改 `isInteracting`，导致鼠标进入段落即展开操作区。
- 段落底部操作区通过 `max-height`、`margin-top` 和 `scale-y` 动画进入文档流，因此会改变段落高度并造成页面跳动。
- 当前操作包括：段落精翻、长难句解构、本地朗读、外教领读、出题测试。
- 选区工具条和右键批注已经存在，本轮保持不变。
- 段落复杂度通过 `isConsideredParagraph` 判断；当前会直接隐藏长难句、朗读与出题功能，本轮改为稳定菜单中显示并对不适用项给出解释。

## 5. 最小实施计划

1. 将 `isInteracting` 收敛为“段落菜单是否打开”，删除鼠标进入即打开的行为。
2. 在段落右侧增加绝对定位 `⋯` 入口，保持低透明度但始终可访问。
3. 用绝对定位菜单替换底部横向按钮组，复用原处理函数。
4. 调整外部点击和 `Esc` 关闭逻辑，确保切换段落时旧菜单关闭。
5. 静态检查旧按钮组已删除、菜单标记存在，并进行 Babel JSX 解析。
6. 更新本日志；保持 PR #8 为 Draft，不合并 `main`。

## 6. 验收标准

- 鼠标进入段落时正文高度不变化。
- 不悬停也能发现并点击段落入口；手机端无需 Hover。
- 点击 `⋯` 后菜单保持打开，鼠标离开不会关闭。
- 点击空白、按 `Esc`、再次点击入口可关闭。
- 点击另一段入口时上一段菜单关闭。
- 菜单不撑开段落、不推动后文。
- 短段落仍显示稳定入口和基础操作；不适用功能有明确说明。
- 原有翻译、长难句、本地朗读、外教领读和出题处理函数继续工作。
- Babel JSX 解析通过。

## 7. 实际修改

开发中。

## 8. 测试

待执行。

## 9. 未完成项

开发中。

## 10. 回滚方式

回退本次功能提交即可；不涉及 `main`。
