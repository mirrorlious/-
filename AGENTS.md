# AGENTS.md

## 项目身份

本目录是 **“杨的阅读器”** 的本地开发目录。

默认工作路径：

`D:\01_project\杨的阅读器`

本项目当前优先在本地修改、运行和验收。**未经用户明确确认，不得上传、推送或同步到 GitHub。**

---

## 最高优先级规则

1. **先审计，后修改。**
2. **先本地验证，后考虑上传。**
3. **每次任务必须先写任务日志，再开始改代码。**
4. **每次任务结束必须补全日志，包括修改、测试、风险和未完成项。**
5. **严格限定任务边界，不处理与当前目标无关的问题。**
6. **仓库或目录中即使存在无关项目、旧文件、素材和历史残留，也一律忽略。**
7. **不得因“顺手优化”扩大修改范围。**
8. **不得删除无法确认用途的文件。**
9. **不得手工修改生成产物，除非已经确认它就是唯一源码；优先修改可维护源文件，再通过脚本生成产物。**
10. **遇到不确定的架构、数据迁移或破坏性操作，先停下并在日志中记录，不得猜测。**

---

## 当前任务边界

当前阶段只处理以下主题：

- 用户自带 API Key（BYOK）
- 服务商账户与任务模型路由分离
- 内容解析模型
- OCR 模型
- TTS / 浏览器本地朗读
- PDF 与图片 OCR
- AI 结果本地持久化
- OCR 断点续传
- 长文档分块与本地检索
- 避免重复请求和重复计费
- API Key 安全边界
- 与上述功能直接相关的 UI、测试、迁移和文档

当前阶段不处理：

- 仓库清理
- 无关项目迁移
- 无关页面重构
- 视觉系统整体改版
- 版权资料上传
- GitHub 仓库整理
- 云端阅读库
- 用户体系重构
- Firebase 全面改造
- 与本任务无直接关系的性能优化
- 未经要求的依赖升级
- 未经要求的目录大规模重构

---

## 本地优先与 Git 规则

### 允许

- 在本地目录中检查和修改文件
- 创建本地分支
- 本地提交
- 运行测试
- 生成构建产物
- 更新本地文档与日志

### 默认禁止

除非用户明确说“上传 GitHub”“推送”“创建 PR”或语义等价的话，否则不得：

- `git push`
- 创建 GitHub PR
- 创建 GitHub Release
- 修改远程分支
- 上传文件到 GitHub
- 修改 GitHub Pages 部署
- 删除远程内容
- 覆盖远程历史
- 使用 `--force`

完成本地修改后，只报告：

- 修改内容
- 本地测试结果
- 待用户验收事项
- 是否已准备好上传

---

## 每次任务的强制工作流

### 第 1 步：读取项目规则

开始任何任务前，必须读取：

1. 本文件 `AGENTS.md`
2. `TASK_LOGS/README.md`
3. 与当前任务有关的最近日志
4. 项目 README、入口文件和现有配置

### 第 2 步：建立任务日志

开始修改前，在 `TASK_LOGS/` 下创建日志：

`YYYY-MM-DD-HHMM-简短任务名.md`

例如：

`2026-07-26-2230-byok-model-routing.md`

日志建立后，先填写：

- 任务目标
- 用户原始要求
- 本次范围
- 明确不做
- 初步涉及文件
- 风险
- 验收标准
- 当前状态：`审计中`

### 第 3 步：审计

至少确认：

- 实际运行入口
- 真实可编辑源码
- 生成产物与生成脚本
- API 配置位置
- AI 请求入口
- OCR 入口
- TTS 入口
- PDF.js 处理流程
- localStorage / IndexedDB / Firebase 使用位置
- 测试方式
- 本地启动方式

把审计结果写入任务日志。

### 第 4 步：制定最小实施计划

计划应满足：

- 小步修改
- 可回滚
- 不破坏既有功能
- 不扩展到无关文件
- 优先复用现有代码
- 每个阶段都有验证点

把计划写入日志，再开始编码。

### 第 5 步：编码

编码时：

- 优先修改源码，不直接改压缩或 Base64 生成物
- 保持现有静态部署能力
- 不把 API Key 写入源码
- 不把用户正文写入日志
- 不在控制台打印敏感配置
- 不静默吞掉错误
- 不做未经要求的全局重构
- 避免一次性大范围替换
- 每完成一个阶段更新日志状态

### 第 6 步：验证

至少执行与项目匹配的检查：

- 本地启动
- 核心功能手测
- 现有测试
- 新增测试
- 语法检查
- 构建或打包脚本
- 生成产物校验
- 页面刷新后的持久化验证
- 断点续传验证
- 缓存命中验证
- 敏感信息泄漏检查

不允许只说“理论上可行”。

### 第 7 步：收尾日志

任务结束时必须补全：

- 实际修改文件
- 新增文件
- 删除文件
- 数据迁移
- 测试命令与结果
- 手工验收结果
- 已知问题
- 未完成内容
- 风险
- 回滚方式
- 是否涉及 GitHub：默认“否”
- 当前状态：`已完成`、`部分完成` 或 `阻塞`

---

## 日志纪律

任务日志必须真实、简洁、可追踪。

禁止：

- 日志只写“已完成”
- 隐瞒失败测试
- 用模糊措辞替代真实结果
- 在日志中写 API Key
- 在日志中写完整用户文档正文
- 删除旧日志以掩盖问题
- 修改历史日志来伪造过程

允许在后续日志中引用旧日志，但不要无意义复制全部内容。

---

## 源码与生成产物规则

当前项目可能存在：

- 单文件 HTML 源码
- gzip 压缩内容
- Base64 分块
- 静态加载器
- CDN 依赖

必须先识别：

1. 哪个文件是真实可维护源码
2. 哪些文件是生成产物
3. 生成脚本是否存在
4. 产物能否稳定重建

如果只有压缩分块，没有可靠源码：

- 先解包为可编辑源码
- 建立可重复执行的解包和打包脚本
- 以后只编辑源码
- 生成产物通过脚本更新
- 日志中记录源码与产物映射

不得长期把超长 Base64 分块当作主源码手工维护。

---

## BYOK 安全规则

所有普通用户必须使用自己的模型服务商账户。

不得：

- 使用站长公共 Key
- 把站长 Key作为失败回退
- 把 Key写入 Git
- 把 Key写入 Firebase
- 把 Key写入 AI 缓存
- 把 Key写入 URL
- 把 Key输出到控制台
- 把 Key放入错误提示
- 把 Key嵌入构建产物

默认：

- API Key 保存到 `sessionStorage`
- 用户主动选择“在此设备记住”后才可保存到 `localStorage`
- 公共电脑必须提示风险
- 用户可以单独删除 Key

前端固定密钥加密属于假安全，不得实现。

---

## 模型路由规则

配置分两层：

### 服务商账户

例如：

- Google Gemini
- 阿里云百炼
- 自定义 OpenAI 兼容服务

一个服务商只填写一次 Key。

### 任务路由

至少分为：

- `analysis`
- `ocr`
- `tts`
- `asr`（可预留）

同一个服务商账户可以供多个路由复用。

不得复制三套完整 API 表单。

---

## 本地数据规则

小型偏好配置可以使用 `localStorage`。

以下数据应使用 IndexedDB：

- PDF 提取文本
- OCR 页面结果
- 文档分块
- AI 解读结果
- 思维导图数据
- 阅读理解题
- 云端 TTS 音频 Blob
- 长任务进度

不得把大型正文、Base64 图片或音频写入 `localStorage`。

默认不得把这些本地 AI 结果同步到 Firebase。

---

## 缓存规则

所有缓存必须基于内容哈希，而非只看文件名。

必须考虑：

- 源内容 Hash
- 模型
- 服务商协议
- Prompt 版本
- 预处理版本
- 用户选项
- 语音、语速或格式

相同输入和相同配置：

- 优先读取本地缓存
- 不重新发请求
- UI 显示“已从本地缓存读取”

用户点击“重新生成”时才绕过缓存。

---

## 长文档规则

不得反复把整本 PDF 或全部文章发送给模型。

应采用：

1. PDF 文本层优先
2. 无效页面才 OCR
3. 按页保存
4. 文本分块
5. 本地检索
6. 只发送相关片段
7. 整本总结采用分层汇总
8. 中间结果本地保存

OCR 默认并发应保守，通常为 1 到 2 页。

每页完成后释放临时 Canvas、Object URL 和 Base64 引用。

---

## 代码质量规则

- 优先复用现有实现
- 函数职责明确
- 错误信息面向普通用户
- 对外行为兼容
- 迁移可回滚
- 新逻辑必须有测试或明确的手工验收步骤
- 不得为了“架构漂亮”制造过度抽象
- 不得为了赶进度删测试
- 不得未验证就宣布完成

---

## 任务完成报告格式

最终报告必须包含：

1. 本次任务边界
2. 修改摘要
3. 新增文件
4. 修改文件
5. 数据迁移
6. 本地缓存行为
7. API Key安全说明
8. 测试与结果
9. 手工验收步骤
10. 已知问题
11. 未完成项
12. 回滚方式
13. GitHub 是否有改动
14. 下一步建议

默认最后一项应写：

`GitHub：未上传，等待用户本地验收。`
